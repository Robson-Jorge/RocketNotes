import styled from 'styled-components'

export const Form = styled.form`
  max-width: 550px;
  margin: 38px auto;

  .tags {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  }
`
